# 任务清单（逐条完成）

## 汇报内容
- 项目成员分工和预期计划  
  - 李曾洋：前端（React/Vite/TanStack Query）、产品流转、性能体验优化；计划本周完成报告页与对比页的导出下载体验和任务清空功能。  
  - 李东旭：后端（FastAPI + Final2x + Redis 队列 + MySQL 持久化）、模型管线、任务队列与指标计算；计划本周完成指标基线修正、任务持久化、部署文档。  
  - 共同：数据回归测试、演示录屏、汇报 PPT。
- 项目的实际进度曲线、燃尽图  
  - 进度：核心链路“上传→处理→对比→报告”已打通，近期集中修复下载/PDF 导出、清空任务、指标展示。  
  - 说明：燃尽图暂未导出，可在汇报前从任务系统导出最新燃尽曲线（RelaiZe/看板）。若需，使用最近一周的完成/剩余工时生成。
- 实际遇到哪些问题，如何解决的？  
  - 指标零提升：后端使用降质基线重新计算 before/after，避免 delta 恒为 0。  
  - 任务清空失败：清理逻辑补充 MySQL + Redis + 磁盘的全量清除。  
  - PDF 导出缺图/包含导出按钮：增加图片预加载、跨域处理与忽略导出栏。  
  - 下载异常：统一下载封装、后端 FileResponse 设置文件名与类型。  
  - 处理 OOM：对大图限制最长边并自动降级，超分 OOM 时跳过超分但保人脸修复。
- 一张觉得效果最好的 SCRUM 会议照片  
  - 待收集：请从本周例会照片中挑选一张清晰的 SCRUM 看板/站会合影，命名 `scrum-best.jpg` 放入汇报材料。
- 项目的实际架构：前后端框架、使用的技术、部署的流程等  
  - 前端：React 18 + TypeScript + Vite 7，React Router 7，TanStack Query 5，Tailwind UI；页面：上传/调参/对比/报告。  
  - 后端：FastAPI + Uvicorn；模型管线 Final2x（RealESRGAN 系列）+ 可选人脸修复（GFPGAN/CodeFormer）；Redis 做队列，MySQL 持久化任务与指标；存储目录本地 uploads/processed/reports。  
  - 部署：启动 Redis + MySQL；后端加载 `.env`（含 `DATABASE_URL`、`REDIS_URL`）；前端 `.env` 指向 API；使用 Uvicorn/pm2 启动后端，Vite 构建前端静态资源。
- 描述项目测试的工作安排、测试工具选择和运用，项目测试评述  
  - 工作安排：每个功能合并前进行手动冒烟（上传/处理/对比/报告/下载/PDF/清空）；大图/人脸场景做回归；并发 5/10 场景观察。  
  - 工具：浏览器 DevTools + 网络面板；后端日志（loguru）；可选 JMeter/ab 做简单并发压测；Redis 队列监控脚本（queue_monitor）。  
  - 评述：核心链路已稳定，仍需补充自动化用例（API 层、前端 e2e）和性能基准记录。
- 各个成员的过程体会  
  - 李曾洋：前端体验优化需要与后端接口契合，下载/PDF 这类边缘流程要早测；Tailwind 设计稿与实际数据长度要兼顾。  
  - 李东旭：指标基线设计影响展示说服力，队列/持久化要覆盖 Redis 断连、DB 落盘、文件清理等异常路径；大图 OOM 需提前降级策略。

## 具体流程
- 需要演示软件，请提前录制好视频
- PPT 演示和作品展示请控制在 30 分钟以内

## 小组互评
- 每小组出 2 个同学进行打分互评，打分为 5 分制  
  5 分：项目完成得非常好，超出预期；4 分：项目圆满完成了任务；3 分：项目任务基本完成，但存在些许不足；2 分：项目完成度较差，分工协作混乱。
- 打分要求：尊重他组，认真打分，实事求是，对所有组采用一个标准，分数能真实反应其他组的报告质量和本组的统计意见，禁止一锅端平的情况。
